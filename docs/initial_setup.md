# 本次初步配置交付

日期：2026-09-19。项目名称：Campus Traffic Signal RL；仓库标识：`campus-traffic-signal-rl`。

## 已完成

- 建立 `.venv`，复用本机已有 PyTorch 等包，安装 SUMO 1.27.1、SUMO-RL 1.4.5、Stable-Baselines3 2.9.0。
- 从零生成单十字路口和显式车辆到达记录，无需购买数据或先做实地采集。
- 实现固定配时、排队优先、DQN 三种策略的共同运行与评估入口。
- 配置均衡、高峰、潮汐三类需求，测试种子 101、102、103。
- 增加最长绿灯约束、最小绿灯和黄灯逐秒检查；验证实际连接的相位映射。
- 保存车辆行程、逐步队列、分方向等待、未完成与未入网车辆数量。
- 补齐实验方案、资源说明、实地记录模板和 PowerShell 安装脚本。

## 实际验证

| 检查 | 结果与证据 |
|---|---|
| 实际 SUMO / TraCI 连接 | 通过；`outputs/environment_check.json` |
| 包依赖一致性 | `pip check` 通过 |
| 针对性测试 | 4 项通过：相位映射、需求可重复性、信号时长与 Gym API、含积压的指标统计 |
| DQN 短程训练 | 1,200 个决策步、175 次梯度更新；参数已变化 |
| 保存与读取模型 | 通过；`outputs/dqn_smoke/model.zip` 与 `run.json` |
| 两种基线快速运行 | 3 场景 × 1 个种子 × 2 策略，每回合 600 秒；`outputs/baseline_check/report.md` |
| 三策略完整运行检查 | 27 回合全部完成，9 份共同需求文件；所有回合的车辆总数核算一致 |

完整初始评估已保存到 `outputs/initial_evaluation/`，包含 `report.md`、`summary.csv` 和复现信息 `run.json`。实际完成 3 场景 × 3 测试种子 × 3 策略，共 27 回合，每回合 1,800 秒。

短程 DQN 仅在均衡车流、每回合 300 秒的配置上检查训练链路。它不是最终模型；与 1,800 秒测试回合也存在时长差异。此轮评估用于检查完整流程和暴露泛化问题，不能作为充分训练后的算法优劣结论。

## 后续最值得做的事

1. 先阅读 README，亲自运行基线并查看队列变化。
2. 用默认 1,800 秒回合完成较长 DQN 训练，再检查独立场景表现。
3. 确定真实路口后，按 `data/field/README.md` 收集少量观测，决定是否加入转弯、行人和非机动车。
4. 选一个明确的改进点深入，例如方向等待公平性，而不是同时增加多个复杂算法。

本次没有采集真实路口数据，也未连接实际交通信号设备。SUMO GUI 演示命令已提供，本次验证使用无界面仿真。
